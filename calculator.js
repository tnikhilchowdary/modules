const math = require('./math.js');

const a = 10;
const b = 5;

console.log(`Add: ${math.add(a, b)}`);
console.log(`Subtract: ${math.sub(a, b)}`);
console.log(`Multiply: ${math.mul(a, b)}`);
console.log(`Divide: ${math.div(a, b)}`);